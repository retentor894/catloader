services:
  backend:
    build: ./backend
    container_name: catloader-dev-backend
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ${TEMP_DIR:-/tmp/catloader-dev}:${TEMP_DIR:-/tmp/catloader-dev}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  frontend:
    build: ./frontend
    container_name: catloader-dev-frontend
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-8180}:80"
    depends_on:
      - backend
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
